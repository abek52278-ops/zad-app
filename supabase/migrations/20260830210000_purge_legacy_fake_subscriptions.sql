-- ═══════════════════════════════════════════════════════════════════════════
-- 20260830210000 — تنظيف اشتراكات المسار القديم (rev 3 — schema-safe).
--
-- الحقيقة المثبتة بالفحص:
-- 1) صفوف الويبهوك المزيفة لم تُكتب أبداً: الـ upsert كان يرسل أعمدة
--    (order_id/purchase_token/tier) غير موجودة في zad_subscriptions أصلاً
--    (baseline 20260722050000) → Postgres كان يرفض الـ insert بـ 42703.
-- 2) الاكتشاف التلقائي (detectSubscriptions) اتصلح في 51378da ليطلب تأكيد
--    المستخدم قبل الكتابة — الصفوف القديمة كتبت قبل الإصلاح بلا علامة تميّز
--    آمنة تفصلها عن الاشتراكات الحقيقية المؤكدة.
--
-- القرار: مفيش حذف بالقوة من الـ migration (خطر مساس ببيانات حقيقية).
-- التنظيف بيتم من داخل التطبيق: شاشة الاشتراكات بتعرض لكل صف زر حذف،
-- والمستخدم هو اللي يحكم أي "اشتراك" مش حقيقي ويمسحه بضغطة.
--
-- الـ migration ده marker/schema guard فقط عشان يطبق نظيف ويسجل التاريخ.
-- ═══════════════════════════════════════════════════════════════════════════

-- أعمدة اختيارية للتتبع — آمنة لو موجودة أصلاً
alter table public.zad_subscriptions
  add column if not exists source text not null default 'user';

comment on column public.zad_subscriptions.source is
  'user = ضافها المستخدم/أكّده | ai_detected = اتكشفت تلقائياً قبل نظام التأكيد (2026-08-30)';
