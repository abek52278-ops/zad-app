*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore
# كل مفاتيح التوقيع — مش debug.keystore بس. مفتاح رفع بيدخل تاريخ git مايتشالش
# منه أبداً (rewrite للتاريخ + إبطال المفتاح عند جوجل)، والسطر ده أرخص من ده بكتير.
# اتكشفت 2026-09-06: `git check-ignore my-upload-key.jks` كان بيرجع "مش متجاهَل"،
# رغم إن CLAUDE.md بيقول keystores متجاهلة.
*.jks
*.keystore
*.p12
*.pepk
# استثناء واحد مقصود: مفتاح الديباج متسجّل في الريبو عن قصد (5e2e77a) عشان بناءات
# الديباج تتوقّع بثبات. مش سر — كلمة السر القياسية `android` ومابيوقّعش أي حاجة
# بتتنشر. الاستثناء مكتوب صراحة عشان لو اتمسح واتضاف تاني ما يتبلعش في صمت.
!zad-debug.keystore

testsprite_tests\tmp\config.json

# Log and debug files
*.txt
!sources.jar
!app/proguard-rules.pro
# Tracked on purpose: checkNoRawColorsInScreens reads this and the build fails
# without it. The blanket *.txt above is meant for scratch logs, not this.
!app/raw-color-baseline.txt
*.log
deploy_result.txt
local.properties
cli_debug.txt
crash.txt
build_log.txt

# Ignored large directories
google-cloud-sdk/
node_modules/
.idea/
testsprite_tests/
google-cloud-sdk/  
node_modules/  
.gradle/  
build/  
supabase/.temp/

# AI tooling
deepseek-vscode-extension/
web_preview/

# ناتج بناء محلي — الـ APK بيتسحب من artifacts الـ CI، مش بيتتبع هنا
build-out/
.aider*

local.properties
