# ZAD V2 — خطة "العقل الواحد"

> **الحالة:** مسوّدة معتمدة — 2026-08-31
> **المصدر:** مسح شامل (أندرويد + إيدج فنكشنز + سوبابيز حي) — كل رقم هنا متحقَّق منه، مش مقروء من دوك قديم.
> **يُقرأ بعد:** `ZAD_MASTER.md` (المعمار والقرارات المقفولة) و `PRODUCT_PLAN.md` (الأولويات).
> **ترقيم المهام:** الملاحم 30+ — الترقيم 1–28 مقفول ومحجوز، مفيش تصادم.

---

## 1. الخلاصة في ثلاث جمل

العقل اللي عايزه **مبني بالفعل** — 5 موظفين متخصصين، صندوق بريد بينهم، جراف ذاكرة بعلاقات، بحث دلالي، كشف تناقض، أهداف، جدولة، تعلّم مهارات. مفيش حاجة من دول محتاجة تتبني من الصفر.

المشكلة إن **معظمه مفصول عن الكهربا**: 9 ملاحظات ذاكرة بـ **صفر** embedding، **صفر** رابط في جراف الذاكرة، **صفر** مهارة متعلّمة، **صفر** هدف، **صفر** التزام ثابت. الشبكة العصبية متوصّلة بس مفيهاش تيار.

والسبب اتكشف: **نمط عطل منهجي** — كود بيسأل الداتابيز عن أعمدة مش موجودة، والخطأ بيتبلع بصمت، فالأداة بتقول للعميل "اتسجل" وهي مسجلتش حاجة.

---

## 2. نمط العطل المنهجي — أهم اكتشاف في المسح كله

ثلاث حالات مؤكَّدة، نفس السبب:

| المكان | الكود بيطلب | الحقيقة | النتيجة |
|---|---|---|---|
| `zad-brain/index.ts:4567` | `zad_inventory.select("id, name, quantity, unit, updated_at")` | العمود اسمه `item_name`، و `updated_at` مش موجود | 42703 → نص "حلم الليل" (إعادة التموين) **ميت من يوم ما اتكتب** |
| `zad-brain/index.ts:1032` | `zad_obligations.select("name,amount,due_date,is_paid").eq("is_paid", false)` | الأعمدة `title`… و `is_paid` **مش موجود أصلاً** | 42703 → `salary_plan` بتحسب الالتزامات = صفر دايماً |
| `zad-brain/index.ts:1020,1070,1107` | `zad_memory.upsert(…, { onConflict: "user_id,scope" })` | مفيش unique constraint — الموجود index عادي بس (`0001_zad_brain.sql:50`)، والـ PK على `id` لوحده | 42P10 → **الشخصية المالية وخطة الراتب والتحدي مش بتتسجل ولا مرة** |

في التلات حالات النتيجة بترجع في متغير مهمَل (`const { data } = …` من غير `error`)، فالموديل بيشوف نجاح، والعميل بيسمع "سجّلتلك" وهو مسجّلش.

**ده مش 3 باجات — ده غياب عقد بين الكود والسكيما.** الإصلاح الحقيقي هو تست في CI بيقارن كل `.from("x").select("a,b,c")` في الإيدج فنكشنز بالأعمدة الفعلية، فالباج ده ميحصلش تاني. ده بند 30.1 وهو أهم بند في الخطة كلها.

---

## 3. الأعطال المؤكَّدة — مرتّبة بالخطورة

### 🔴 أمان

1. **`zad_media_pass_grant` مفتوحة لـ `anon`** — `SECURITY DEFINER`، وجواها `uid := coalesce(auth.uid(), p_user)`. الحارس اللي جواها (`if auth.uid() is not null and p_user <> auth.uid()`) بيشتغل بس للمسجَّلين. يعني **أي حد من غير تسجيل دخول** يقدر ينده الـ RPC بـ uuid أي عميل ويديله (أو يحرقله) اشتراك وسائط 24 ساعة. متحقَّق منه حي: `anon_exec = true`.
2. **7 دوال `SECURITY DEFINER` تانية مفتوحة لـ `anon`** — `_agent_bill_reminder_for_user`, `_agent_home_weekly_digest_for_user`, `_agent_listener_gap_alert_for_user`, `_agent_warranty_reminder_for_user`, `agent_goal_touch_progress`, `zad_skill_touch`, `zad_skill_upsert`. الريبو فيه **3 ميجريشنز سابقة** بتقفل بالظبط النوع ده — فدي دوال اتضافت بعد الكنسة ومتقفلتش. المشكلة تنظيمية مش تصميمية.
3. **6 دوال `p_user uuid` مفتوحة لـ `authenticated`** لازم تتراجع أجسامها تتأكد إنها بتقارن `p_user = auth.uid()` — لو لأ، دي قراءة/كتابة عابرة للحسابات.
4. **3 جداول RLS مفعّل من غير أي policy**: `dashboard_admins`, `zad_ad_grants`, `zad_orphaned_rows`. دلوقتي fail-closed (مأمّن)، لكن منح CRUD الكاملة لـ `anon`/`authenticated` لسه قاعدة عليها — أول policy تتضاف أو أول `DISABLE RLS` وقت ديباج، الجداول دي تبقى مكتوبة للعالم. و `dashboard_admins` هو الجدول اللي بيقرر مين أدمن.
5. `zad_notif_fuzzy_key` + `zad_notif_fuzzy_key_fill` من غير `SET search_path`.
6. حماية الباسوردات المسرّبة (HaveIBeenPwned) مقفولة في إعدادات Auth.

### 🔴 وظيفي

7. **الذاكرة الدلالية ميتة** — `zad_memory.embedding` = 0 من 9. الكود بينده `embedText` ثم `zad_memory_set_embedding` (`index.ts:1135-1136`) صح، والفهرس ivfflat موجود. فالشك الأول على **`text-embedding-004` نفسه** — ده بالظبط نفس فخ `gemini-2.5-flash` اللي `CLAUDE.md` محذّر منه: الموديل بيظهر في `ListModels` وبيرجع 404 عند النداء. **لازم probe حي قبل أي إصلاح.**
8. **`verify-purchase` بيرمي `ReferenceError` في كل عملية شراء ناجحة** — المتغير `pid` مستخدَم في السطور 133–136 و**معرَّفش في الملف خالص** (متأكد بالـ grep). السطر 136 غير مشروط، يعني بعد ما جوجل تأكد الشراء، الفنكشن بيرجع `internal_error`. **العميل بيدفع وميخدش حاجة.**
9. `zad-billing-webhook` بيكتب أعمدة مش موجودة في `zad_subscriptions` (نفس نمط 42703).
10. `agent_turn_stream` بينتظر الرد كامل بعدين بيقطّعه 24 حرف — **ستريمنج مزيّف**، مفيش توليد تدريجي.
11. `zad_users` = 6 و `auth.users` = 4 → صفّين يتامى.

### 🟠 أدوات متقطعة عن بعضها

12. `delete_inventory_item` موجودة في `TOOLS` ومش في `CHAT_TOOLS` → **العميل مش قادر يقول "امسح الصنف ده" في الشات**.
13. `family_mediation` وصفها بيقول "نادِها لما العميل يشكك" لكنها في الحلقة الخلفية بس → **الشات عمره ما هينديها**.
14. `link_memory` أداة اختيارية بيقرر الموديل ينديها → عمره ما نداها (0 روابط). لازم تبقى **تلقائية عند الكتابة**، مش قرار موديل.

---

## 4. الكذب في الواجهة — القائمة الكاملة

`ZAD_MASTER.md` كاتب بنفسه: *"كارت بيكدب أسوأ من كارت غايب."* دي الكروت اللي بتكدب:

| # | المكان | الكذبة |
|---|---|---|
| 1 | `ZadHomeGlanceCards.kt:928-933` | شارات الكرة العصبية: `412` عقدة، `2,103` رابط، `+196%` نمو، `96%` دقة — **حروف مكتوبة**، الودجت مش بياخد أي داتا أصلاً (`:814-819` كولباكات بس) |
| 2 | `ZadHomeGlanceCards.kt:851-861` | حالة كل عقدة نص ثابت: "كفاية المخزون: 18 يوماً"، "الالتزام الدوائي: 91%"، "دقة التوقع: 94.2%" |
| 3 | `ZadHomeGlanceCards.kt:1180-1185` | التقرير التنفيذي: `totalSpent = 3250.0`, `safeDailySpend = 145.0`, `forecast = 4100.0`, `adherence = 91` |
| 4 | `ZadHomeGlanceCards.kt:1253-1257, 1281, 1328-1330` | سرد مالي مخترع: "كبح الاندفاع 88/100"، "تفادي 4 عمليات شراء عاطفية"، "وفّر 450 ر.س" — **لكل العملاء بنفس النص** |
| 5 | `ZadHomeGlanceCards.kt:1236, 1355-1364` | **ختم توثيق مزيّف**: "كود التوثيق #ZAD-NEURAL-8841" و "ZAD NEURAL VERIFIED & DIGITALLY SIGNED" — ده الأخطر، بيدّي غطاء رسمي لأرقام مخترعة، وفيه زرار مشاركة بيصدّرها بره |
| 6 | `MarketRadarRepo.kt:36-174` | كل أسعار الذهب والبنزين والخضار **ثوابت مكتوبة** بنِسَب تغيّر مخترعة، وفوقها لافتة **"تحديث لحظي"**. وللبلاد غير المدرجة: `عيار 24 = 100.0` |
| 7 | `ZadAiRepository.kt:218-340` | 5 وصفات ثابتة بتكاليف مخترعة، وبعدين النص بيتكتب: *"جمعتلك أفكار وصفات… من المخزون المتاح عندك"* — العميل مش قادر يفرّق بين ذكاء وقالب |
| 8 | `ZadViewModel.kt:4716-4771` | 6 منتجات أفلييت بصور Pexels وأسعار ريال مخترعة، بتظهر كـ fallback في 3 أماكن |

**الخبر الحلو:** رقم 6 مش محتاج بناء — `fetch_live_market_prices` موجود وشغال في `zad-core-intelligence` (ذهب 21/24 + بنزين 91/95، كاش 12 ساعة). ده **توصيل سلك، مش ميزة جديدة**.

**ورقم 1 و 2 و 3 هما بالظبط "صفحة عقل زاد"** اللي طالبها — الأرقام الحقيقية موجودة كلها في الداتابيز. التفاصيل في ملحمة 35.

---

## 5. رؤيتك مقابل الواقع — ميزة ميزة

| ما تريده | الموجود | الفجوة الحقيقية |
|---|---|---|
| يسجّل المبلغ في الكارت الأخضر | ✅ `BudgetGateScreen` إجباري + `zad_budget_state()` سلطة واحدة | الرقم ناقص: `zad_obligations` **فاضي**، و"المتاح" بيطرح صفر |
| مخزون بالكاميرا/الفاتورة → حقن صح | ✅ `analyze_receipt` + `analyze_inventory_image` + `InventoryFlowEngine` (تطبيع عربي، مطابقة ضبابية، رفض أسماء مش منتجات) | ممتاز فعلاً. الناقص: الفاتورة متبقاش لقطة — تغذّي معدل الاستهلاك |
| النواقص تتسجل | ✅ عتبة + انتهاء صلاحية + `predictStockDepletion` | مبنية على مطابقة عناوين المعاملات، وفيه `zad_consumption` أدق ومستخدم بنص طاقته |
| شيف زاد احترافي | ✅ `meal_suggestions` من مخزون حقيقي | 5 قوالب ثابتة بتتقدّم كأنها ذكاء (§4.7) + الشيف مش عارف الميزانية ولا الدوا ولا الحساسية |
| نوتفيكيشن فيزا → الإيجنت يكلمني | ✅ `UnifiedBankListener` (~180 تطبيق) + `zad_transaction_proposals` + تأكيد إجباري | التسليم ضعيف: بيروح insight/تليجرام. **مفيش بوش فوري ولا صوت ولا كارت محادثة** |
| الكارت يتعدّل بدقة | ✅ `BudgetMath` + `balance_anchored_at` + `Figure(confident)` | شغال. بس ناقصه الالتزامات (فوق) |
| تابي وتمارة والأقساط | ✅ متعرَّف عليها في الپارسر (`SaBankParser.kt:380,463-464,583`) وفي أدوات الشات | **مفيش أوتوماتيكية**: القسط بيتحوّل معاملة، لكن مبيتحوّلش التزام متكرر. علشان كده `zad_obligations` فاضي |
| يتنبأ ويتعلم من سلوكي | ✅ `user_behavior_profile` + `zad_consumption` + `ConsumptionLearner` + كرون يومي | التعلّم بيتخزّن ومبيرجعش للبرومبت بشكل منظّم |
| البوت يسأل ويسجّل | ✅ `zad-telegram-bot` بـ grammY + 4 جداول تأكيد | شغال. الناقص: البوت لسه مش "بوابة كل حاجة" — الصوت والصور بيروحوا لـ core-intelligence مباشرة، بيلفّوا حول العقل |
| ذاكرة طويلة المدى تكبر | ✅ `zad_memory` + `zad_memory_links` + pgvector + كشف تناقض + `zad_skills` | **البنية أقوى حاجة في المشروع كله وهي ميتة**: 0 embedding، 0 رابط، 0 مهارة |
| شبكة عصبية، موظفين لكل ميزة | ✅ `specialists.ts` (5 متخصصين + راوتر + قصر الأدوات) + `zad_agent_messages` | موجود وشغال. محتاج توسعة (صيدلي، صيانة، عائلة) وذاكرة مشتركة |
| مساعد صوتي بنت بمشاعر | ✅ ElevenLabs + `VoicePersona` + "يا زاد" wake word | **بالدور، 3-5 ثواني**. مفيش مقاطعة ولا نبرة متغيّرة. القرار: Gemini Live API |
| لهجة وبنوك كل بلد | ✅ `persona.ts` (20+ لهجة) + `zad_locale_config` + الپارسر متعدد البلاد | الپارسر بيقول بنفسه إن بلاد المرحلة 2 **تخمين متجرَّبش** |
| العائلة: مخزون موحّد + شبكات تتكلم | ✅ 13 جدول عائلة + realtime + `parent-digest` | المخزون **فردي مش عائلي** — الأم والأب مخزونين منفصلين. أكبر فجوة معمارية في طلبك |
| صفحة عقل زاد احترافية | ✅ `ZadIntelligenceScreen` (4006 سطر، ~25 كارت) | فيها أكبر تجمّع كذب (§4) |
| واجهة آيفون بريميوم | 🟡 نصّ الطريق | نظامين توكنز متعارضين + الخط الأساسي بيرجع لخط النظام |

---

## 6. الخطة — 7 ملاحم

### الملحمة 30 — نزع الكذب وإصلاح الأسلاك المقطوعة `[أول حاجة، مفيش نقاش]`

*مفيش ميزة جديدة تتبني فوق ذاكرة مش بتكتب.*

| # | البند | الملف |
|---|---|---|
| 30.1 | **تست عقد السكيما في CI** — يستخرج كل `.from("x").select(...)` من الإيدج فنكشنز ويقارنها بالأعمدة الفعلية. يفشل البناء لو عمود مش موجود | `supabase/functions/**/schema_contract_test.ts` |
| 30.2 | إصلاح التلات حالات المكتشفة: أعمدة `zad_inventory` في حلم الليل، أعمدة `zad_obligations` في `salary_plan`، والـ 3 upserts | `zad-brain/index.ts:4567, 1032, 1020/1070/1107` |
| 30.3 | ميجريشن: `unique (user_id, scope)` على `zad_memory` — بعد دمج المكرر لو موجود | ميجريشن جديدة |
| 30.4 | **قفل الأمان**: `revoke execute` من `anon` على الـ 8 دوال؛ مراجعة الـ 6 دوال `p_user` وفرض `p_user = auth.uid()`؛ `revoke all` على التلات جداول + policies صريحة؛ `search_path` للدالتين؛ تفعيل حماية الباسوردات | ميجريشن جديدة |
| 30.5 | **probe حي لـ `text-embedding-004`** قبل أي كود. لو 404 → نجرب البدائل ونثبّت الشغّال. بعدها backfill للـ 9 ملاحظات | `callModel.ts:752` |
| 30.6 | إصلاح `verify-purchase` (`pid`) + `zad-billing-webhook` (الأعمدة). **العملاء بيدفعوا وبياخدوا `internal_error`** | `verify-purchase/index.ts:133-136` |
| 30.7 | **نزع الكروت الكاذبة الـ 8** — كل واحد يبقى: رقم حقيقي، أو حالة فاضية شريفة. الختم المزيّف يتشال بالكامل | §4 |
| 30.8 | توصيل `MarketRadarRepo` بـ `fetch_live_market_prices` الحقيقي + وسم "آخر تحديث" حقيقي | `MarketRadarRepo.kt` |
| 30.9 | وصفات الشيف الثابتة: تتوسم `source = "template"` في الـ UI أو تتشال | `ZadAiRepository.kt:218-340` |
| 30.10 | `delete_inventory_item` و `family_mediation` تتضاف لـ `CHAT_TOOLS` | `zad-brain/index.ts` |

**التحقق:** `deno check` + `deno test` محلياً (Deno 2.9.5 مركّب)، بعدين CI. لأي حاجة داتابيز: `execute_sql` قراءة قبل وبعد.

---

### الملحمة 31 — تشغيل العقل: الذاكرة تكبر لوحدها

الهدف: الذاكرة تكبر من غير ما الموديل يفتكر ينده أداة.

- **31.1 ربط تلقائي** — بعد كل `remember` ناجح، ندوّر دلالياً على أقرب 3 ملاحظات ونكتب `zad_memory_links` أوتوماتيك بعلاقة مستنتجة. الجراف يبني نفسه. (السبب المباشر لـ 0 روابط)
- **31.2 استخلاص بعد كل جلسة** — بعد كل `agent_turn` ناجحة، تمريرة قصيرة رخيصة تسأل: "إيه الحقيقة الدائمة اللي اتعلمناها؟" وتكتبها. النهاردة الذاكرة بتتكتب لما الموديل يقرر بس.
- **31.3 إصلاح حلم الليل** — بعد 30.2 يشتغل. نزوّد: تعزيز الروابط، تقليم المتناقض، وتلخيص أسبوعي لملاحظة "شخصية" واحدة.
- **31.4 استدعاء مرتّب** — النهاردة الاستدعاء هجين (كلمات ×2 + ثقة + أدلة، وبعدين إعادة ترتيب دلالي). نضيف: حداثة، ونتيجة الجراف (ملاحظة مربوطة بمرشح قوي بترتفع).
- **31.5 تفعيل `zad_skills`** — 0 مهارة يعني `learn_skill` عمره ما اتنده. نفس علاج 31.2: نستخلص المهارة من الأنماط المتكررة بدل ما نستنى الموديل.
- **31.6 توسيع الموظفين** — من 5 لـ 8 متخصصين (يضاف: صيدلي، صيانة/بيت، تفاوض عائلي)، وكل متخصص يقرا بريد المتخصصين التانيين قبل ما يرد. ده اللي بيخلي "الشبكات تتكلم مع بعضها".

---

### الملحمة 32 — الحلقة الكاملة (الكارت ↔ النوتفيكيشن ↔ المخزون ↔ الشيف)

القصة اللي حكيتها، من غير قطع:

- **32.1 التزامات تلقائية** — لما الپارسر يشوف تابي/تمارة/فاليو/قسط **مرتين**، الإيجنت يقترح `zad_obligations` متكرر (باقتراح، مش كتابة صامتة). ده اللي هيملأ الجدول الفاضي ويخلي "المتاح" صح.
- **32.2 المحادثة عند السحب** — النوتفيكيشن يولّد **بوش فوري + كارت محادثة في التطبيق + إعلان صوتي اختياري**، مش insight صامت. `zad_fcm_tokens` و `push.ts` جاهزين ومستخدمين ولا مرة.
- **32.3 الفاتورة تغذّي الاستهلاك** — كل حقن سكان يكتب `zad_inventory_observations` ويعيد حساب `zad_consumption`. بعد فاتورتين "كفاية المخزون بالأيام" تبقى قياس مش تخمين.
- **32.4 شيف زاد يشوف كل حاجة** — الوصفات تتقيّد بـ: المخزون + المتاح فلوس + `zad_pharmacy_items` (تعارض/حمية) + عدد العائلة + الموسم. ده اللي يخلّيه "شيف متخصص" بدل مولّد وصفات.
- **32.5 نواقص → عربة** — النواقص تتجمّع في قايمة مشتريات مرتبة بأولوية وسعر متوقّع من `estimate_price` الحقيقي.

---

### الملحمة 33 — الصوت الريل-تايم `[قرارك: Gemini Live API]`

- **33.1 relay على Supabase Edge** — Deno بيدعم ترقية WebSocket. فنكشن `zad-voice-live` يمسك الجلسة، يمرّر PCM ثنائي الاتجاه، والمفتاح يفضل سيرفر-سايد.
- **33.2 أدوات العقل داخل الجلسة الصوتية** — الـ Live API بيدعم function calling. المتحدثة تنده نفس أدوات `zad-brain` وهي بتتكلم، فـ"سجّلي 50 جنيه قهوة" تتنفّذ **أثناء الكلام** مش بعده.
- **33.3 الشخصية** — نبرة متغيّرة حسب الموقف (مبسوطة/حذرة/جادة)، مقاطعة، وقفات طبيعية. `persona.ts` بيدّي اللهجة بالفعل — نوصّلها بالصوت.
- **33.4 البوابة الحقيقية** — كل الوسائط (صوت/صورة/ملف) تعدّي على `zad-brain` مش تلفّ حواليه. دلوقتي البوت بيبعت الصوت والصور لـ `zad-core-intelligence` مباشرة، فالعقل مش شايفها ومش فاكرها.
- **33.5 مصاريف** — الـ Live API غالي. يتقفل بـ `entitlement.ts` الموجود، وبمهلة صمت.

> **ملاحظة صادقة:** طلبت "إرادة حرة" و"مشاعر". اللي نقدر نبنيه فعلاً: شخصية ثابتة، ذاكرة بتفتكر، نبرة بتتغيّر، ومبادرة (بتفتح هي الكلام). ده اللي بيخلّي الناس تحس إنها بتكلم حد. مش هنوعد بحاجة تانية.

---

### الملحمة 34 — العائلة كشبكة واحدة `[أكبر شغل معماري]`

- **34.1 المخزون العائلي** — أهم بند: `zad_inventory` مربوط بـ `user_id` بس. لازم `family_id` اختياري + RLS بـ `get_my_family_ids()` (النمط موجود). الأم تصوّر الفاتورة والأب يشوف المخزون اتحدّث.
- **34.2 ذاكرة عائلية** — نطاق `family` في `zad_memory`: حقائق مشتركة (حساسية الولد، عيد ميلاد، عادة رمضان) يشوفها كل عقول العائلة، والحقائق الفردية تفضل خاصة.
- **34.3 تقرير الأب** — `parent-digest` بيشتغل أسبوعياً و `zad_parent_digests` **فاضي** → يتشاف في اللوجات ويتصلّح. يتوسّع: صرف + التزام دوائي + مهام + إشارات سلوك.
- **34.4 تنبؤ عائلي** — الأنماط الموسمية تتحسب على مستوى العائلة (`seasonal_forecast` بيعمل ده وبيقع على ثوابت لما التاريخ قليل — بعد 34.1 هيبقى عنده تاريخ كفاية).

---

### الملحمة 35 — صفحة عقل زاد: من HUD كاذب لشركة تحليل

نفس الشكل، أرقام حقيقية:

| الشارة | مصدرها الحقيقي |
|---|---|
| العقد النشطة | `count(zad_memory) + count(zad_skills) + الكيانات المتتبَّعة` |
| الروابط العصبية | `count(zad_memory_links)` — هيبقى حقيقي بعد 31.1 |
| معدل النمو | فرق العدّ عن 30 يوم فاتوا |
| دقة التنبؤ | نسبة قبول المقترحات من `zad_transaction_proposals` + `agent_drift_events` |
| كفاية المخزون | `zad_consumption` بعد 32.3 |
| الالتزام الدوائي | `zad_pharmacy_doses` |

- **35.1** توصيل الشارات والعقد بالحقيقة. أي رقم بلا مصدر يظهر "لسه مفيش" — نفس نمط `NeuralMeshLivePanel` اللي شغال صح بالفعل.
- **35.2** التقرير التنفيذي: يتبني من أرقام العميل، **الختم المزيّف يتشال**، وكل ادعاء يشاور على المعاملات اللي بنته.
- **35.3** جراف الذاكرة يتعرض تفاعلي — `ZadKnowledgeMapScreen` موجود وشغال صح؛ يتوسّع يعرض جراف `zad_memory_links`. **دي أقوى شاشة "بيع" في التطبيق**: العميل يشوف عقله.
- **35.4** كل كارت تحليل يجاوب: **إيه؟ ليه؟ أعمل إيه؟** — مش رقم لوحده.

---

### الملحمة 36 — الواجهة البريميوم

- **36.1 توكن واحد** — `Color.kt` (كانفاس `#ECEEF1`) و `ZadV3` (كانفاس `#F2F2F7`) بيتخانقوا والاتنين مستوردين. `ZadV3` يبقى المصدر الوحيد.
- **36.2 الخط — أهم بند بصري** — `bodyLarge/bodyMedium/labelMedium/labelSmall` (أغلب البكسلات) بترجع لخط النظام. تتربط بـ Cairo/Inter. وكمان `displayLarge` بيطلب `FontWeight.Black` وهو **مش مسجَّل**، فبيتولّد صناعياً ويطلع وحش. يتسجّل أو يتغيّر.
- **36.3 مكونات كوبرتينو** — `AlertDialog`/`ModalBottomSheet`/`Snackbar` الخام تتلف في `ZadCupertinoControls` (موجود، 140 سطر بس).
- **36.4 لغة أيقونات واحدة** — دلوقتي 3 (إيموجي + Lucide + Material). الإيموجي يفضل في وضع الأطفال والتصنيفات، ويخرج من الأسطح البريميوم (`ZadWalletHeroCard.kt:113` فيه `"💳"` جوه الكارت الأخضر).
- **36.5 حركة موحّدة** — `ZadSprings`/`ZadTransitions` موجودين؛ يتفرضوا بدل منحنيات لكل شاشة.
- **36.6 قرار الوضع الداكن** — مقفول إجبارياً بينما التطبيق بيشحن أسطح داكنة ثابتة (`#052E16`, `#0F172A`). يا نعمله بجد يا نبرّره.

---

## 7. أفكار تقوّي كل ميزة

**الكارت الأخضر** — خط زمني للرصيد لآخر الدورة بيتحرك مع كل معاملة؛ "لو صرفت كده هتقف يوم 22"؛ فصل بصري بين *معاك* و*محجوز* و*متاح*.

**المخزون** — كفاية بالأيام مش بالعدد؛ رادار انتهاء صلاحية بيقترح وصفة تستهلك المعرّض؛ "الفاتورة دي أغلى 18% من آخر مرة" (من الملاحظات).

**شيف زاد** — يعرف الميزانية (وصفة تناسب المتاح)؛ يعرف الدوا؛ يتعلم اللي رفضته؛ يبني قايمة أسبوع من المخزون + النواقص.

**الاشتراكات** — "دفعت 3 شهور ومفتحتوش"؛ خط زمني لتابي/تمارة بيوري الشهور الجاية؛ يحذّر قبل التجديد بـ 3 أيام بمبلغ محدد.

**الصيدلية** — يربط الدوا بالمعاملة (يشوف السعر بيغلى)؛ يتنبأ الخلوص من `zad_pharmacy_doses` مش من العدّ؛ يحذّر التعارض عند إضافة دوا.

**البوت** — يبعت **صورة الكارت الأخضر** مش نص؛ سؤال يومي واحد بس (اللي مش عارفه)؛ يقبل الفويس نوت ويسجّل منه.

**العائلة** — "الأم اشترت زيت والأب كمان النهاردة" (`family_mediation` موجودة ومحبوسة)؛ لوحة الأولاد؛ مخزون موحّد.

**الموقع** — `find_nearby_stores` بيرفض الموقع الأقدم من 6 ساعات (كويس). يتوسّع: "إنت جنب كارفور وناقصك 6 حاجات".

---

## 8. الترتيب الزمني

| # | الملحمة | ليه الترتيب ده |
|---|---|---|
| 1 | **30** — الأمان والأسلاك | ثغرة `anon` مفتوحة، والعملاء بيدفعوا وميخدوش. مفيش حاجة تسبق ده |
| 2 | **31** — تشغيل العقل | كل حاجة بعديها بتقف على ذاكرة بتكتب |
| 3 | **32** — الحلقة الكاملة | القصة الأساسية بتاعتك |
| 4 | **35** — عقل زاد | الأرقام بقت حقيقية بعد 31 و32، فالصفحة تتبني على حق |
| 5 | **36** — الواجهة | بعد ما المحتوى يبقى حقيقي |
| 6 | **33** — الصوت | مستقل، ممكن يتوازى مع 35/36 |
| 7 | **34** — العائلة | أكبر شغل معماري، محتاج 30–32 مستقرين |

**قواعد التنفيذ (من `CLAUDE.md`، سارية):** مهمة واحدة → كوميت واحد → تقرير → وقوف. مفيش "خلص" من غير مخرجات بناء بعد التغيير. الإيدج فنكشنز: `deno check` + `deno test` محلياً ثم CI. **مفيش Android SDK هنا** — الكوتلن CI بيتحقق منه، مش أنا.

---

## 9. حاجتان محتاجين قرارك عند الوصول لهم

1. **`PRODUCT_PLAN.md §6`** بيرفض صراحةً: جراف المعرفة، الـ embeddings للذاكرة، التحديات المالية، والـ STT العربي سيرفر-سايد. **الأربعة اتبنوا فعلاً** والدوك متحدّثش. خطة V2 دي بتوسّعهم أكتر. لازم `PRODUCT_PLAN.md` يتصحّح صراحةً — القرار المكتوب ميبقاش بيناقض الكود المشحون.
2. **`PROJECT_MAP.md` بايت وخطر** — لسه بيقول OpenRouter بيشغّل "كل ميزات الذكاء" و 24 أكشن. الحقيقة: Gemini pool بـ 7 موديلات failover و **31** أكشن. يتصلّح أو يتوسم بايت.

---

## 10. أول 3 حاجات لو بدأنا بكرة

1. **30.4** — قفل ثغرة `anon`. نص ساعة، ميجريشن واحدة.
2. **30.6** — `verify-purchase`. سطر واحد. **عملاء بيدفعوا فلوس دلوقتي وبياخدوا `internal_error`.**
3. **30.5** — probe لـ `text-embedding-004`. لغاية ما نعرف إذا كان شغال، الذاكرة الدلالية كلها مبنية على افتراض.
