# خطة إعادة البناء — 2026-08-06

ملف الحالة الجاري لمسار "الأرقام الحقيقية + الواجهة العالمية". **يتحدّث مع كل مرحلة تخلص**
(زي `SESSION_2026_07_30_phaseA.md` بالظبط)، عشان أي جلسة جديدة تبدأ عارفة إحنا فين
من غير ما تعيد استنتاج الحكاية من الـ commits.

الخطة دي **مش** بديلة لـ `PRODUCT_PLAN.md` ولا لترقيم التاسكات في `ZAD_MASTER.md` —
هي ترتيب تنفيذ عرضي فوقهم، جاي من شكوى المستخدم: "كل البيانات وهمية والأرقام غلط".

## قرارات مثبّتة (اتحسمت 2026-08-06)

- الواجهة: **بناء من الصفر بـ Figma MCP** على iOS HIG + Material 3 — الـ mockup القديم
  (`ZAD App.dc.html`) مش موجود في الريبو ومش هيتنطر. `web_preview/index.html` مرجع تاريخي بس.
- ترتيب البدء: **مرحلة ٠ الأول**، وبعدها مرحلة ١ على طول.
- الفلوس تفضل `Double` مع `asMoney()` — مفيش هجرة minor-units (قاعدة CLAUDE.md).

---

## مرحلة ٠ — أوقف نزيف الأرقام · ✅ خلصت

المشكلة: التطبيق بيعرض أصفار وأرقام افتراضية كأنها حقيقة، ونص للمطوّر ظاهر للمستخدم.

| # | البند | الحالة |
|---|------|--------|
| ٠أ | `budget_save_note` ("سيتم حفظ الميزانية في Supabase → جدول zad_users") يتشال من الـ ٤ ملفات strings ويتحول لرسالة للمستخدم | ✅ |
| ٠ب | `BudgetMath.remaining` (وأخواتها) ترجع `null` بدل `0.0` لما `monthlyLimit <= 0` | ✅ |
| ٠ج | بوابة إجبارية للسقف بعد الدخول — مفيش أي رقم قبل ما المستخدم يحدد سقفه | ✅ |
| ٠د | تحديث `BudgetMathTest` + compile + unit tests | ✅ — `:app:compileDebugKotlin` نظيف، `:app:testDebugUnitTest` = 134/134 ناجح (28 منهم BudgetMathTest) |

تفاصيل ٠ب — الدوال اللي بقت `Double?`: `remaining`، `remainingInCycle`،
`dailyAllowanceInCycle`، `velocityInCycle`، `availableInCycle`. المستهلكين اللي اتعدّلوا:
`ZadViewModel._remainingBalance`/`_availableFigure` (بقوا nullable)، `BudgetScreen`،
`HomeScreen`، `ZadVoiceFab`، `FamilyState`، `BudgetTracker`، `ZadCentralBrain`
(`BrainReport.remaining` بقى `Double?`).

تفاصيل ٠ج — البوابة في `MainScreen` (مش في `OnboardingScreen`، ده carousel قبل الدخول):
`ZadViewModel.budgetLoaded` علشان مايحصلش وميض لمستخدم سقفه متأكد، وبعدين
`BudgetGateScreen` لحد ما `updateBudget()` تتنادى. مفيش زرار تخطي — إجباري بقرار المستخدم.

## مرحلة ١ — العملة والدولة الحقيقية · ✅ خلصت (مع فجوة موثّقة)

| # | البند | الحالة |
|---|------|--------|
| ١أ | عمود `zad_transactions.currency` — Supabase migration (`zad_transactions_currency`) + Room `MIGRATION_14_15` (v14→v15) + `ZadTransaction.currency` | ✅ |
| ١ب | `SaBankParser.extractCurrency()` يستخرج العملة من نص الرسالة نفسها (يعيد استخدام `CUR` regex الموجود، `SaBankParser.kt`) بدل افتراض ريال، متسلّك في `detectAndParse` | ✅ |
| ١ج | `BankRulesEngine.tryParse` يستنتج العملة من بلد القاعدة (`rule.country`، القواعد أصلاً مفلترة بالسوق النشط) | ✅ |
| ١د | `CurrencyFormatter.format(context, tx: ZadTransaction)` overload — يعرض عملة المعاملة، مش عملة الـ Market الحالي؛ يسقط لعملة الـ Market لو `tx.currency == null` (صفوف قديمة) | ✅ |
| ١ه | `UnifiedBankListener` يمرر `parsed.currency` لـ `ZadTransaction` عند الحفظ | ✅ |
| ١و | كشف سفر: `TravelDetector` (`networkCountryIso` + fallback `Locale`) + `TravelBanner` composable في `HomeScreen` — اقتراح تحويل قابل للتجاهل، مش تبديل صامت | ✅ |
| ١ز | اختبارات: `SaBankParserTest` (+4 `extractCurrency`)، `BankRulesEngineTest` (+تأكيد `EGP`)، `CurrencyFormatterTest` جديد (4) | ✅ — 142/142 |

**فجوة موثّقة، مش مقفولة**: مسار الـ AI fallback (`ZadAiRepository.analyzeBankNotification` في
`UnifiedBankListener.kt`، بيتفعّل لما `SaBankParser`/`BankRulesEngine` الاتنين يرجّعوا
null) بيرجّع `ZadTransaction` من غير `currency` — بيسقط لعملة الـ Market الحالي زي
السلوك القديم، مش كذب لكنه مش "عملة حقيقية" كمان. يحتاج تعديل الـ Edge Function
(`zad-core-intelligence`) عشان يستخرج العملة كمان، خارج نطاق تعديل كوتلن-فقط. مرشّح
لمرحلة لاحقة أو لتاسك منفصل.

**غير منفّذ عمداً** (خارج نطاق "عملة حقيقية"، جزء من مرحلة ٢): توسيع `Market` enum
لأسواق تانية غير السعودية/مصر/تركيا — `TravelDetector.suggestedMarketFor` بيرجع null
لأي بلد مكتشف مش من التلاتة دول لسه.

## مرحلة ٢ — الشرق الأوسط + البرسونا · ✅ خلصت (مع فجوتين موثّقتين)

| # | البند | الحالة |
|---|------|--------|
| ٢أ | `Market` اتوسّعت من ٣ لـ ١٩ سوق: السعودية، مصر، الإمارات، الكويت، قطر، البحرين، عُمان، الأردن، لبنان، العراق، سوريا، اليمن، فلسطين، ليبيا، السودان، المغرب، تونس، الجزائر، تركيا — كل واحد بـ `countryCode`/`currencyCode`/`currencySymbol`/`flagEmoji` | ✅ |
| ٢ب | `dialectInstruction` مخصّص لكل سوق (مش نسخة عامة "عربي") — خليجي (سعودي/إماراتي/كويتي/قطري/بحريني/عُماني، بكلمات محلية مختلفة لكل واحدة)، مصري، شامي (أردني/لبناني/سوري/فلسطيني)، عراقي، يمني، سوداني، مغاربي (ليبي/مغربي/تونسي/جزائري)، تركي | ✅ |
| ٢ج | `CycleMath.weekendDays` — تركيا/المغرب/تونس/الجزائر/لبنان سبت-حد، الباقي جمعة-سبت | ✅ |
| ٢د | `MarketPickerGrid` مكوّن مشترك جديد (`LazyVerticalGrid` بـ٣ أعمدة + بحث بالاسم/كود العملة + علم) — بيحل محل `Row` اللي كانت هتتكسر مع ١٩ عنصر متساوي العرض | ✅ |
| ٢ه | مربوط في `MarketSelectionScreen` (onboarding) و`ProfileSubScreens`'s "البلد والعملة" مع بعض — نفس المكوّن بالظبط، مش نسختين | ✅ |
| ٢و | `SaBankParser.CUR`/`extractCurrency` موسّعة لكل عملات الـ١٦ سوق الجديد، بما فيها **حل غموض بدون تخمين**: كلمات زي "دينار"/"ريال"/"درهم"/"جنيه"/"ليرة" مشتركة بين أكتر من بلد — بترجع عملة بس لو بتطابق عملة السوق المختار يدوياً حالياً (`MarketPrefs`)، وإلا `null` (رفض، مش تخمين عبر حدود) | ✅ |
| ٢ز | `BankRulesEngine.countryToCurrency`، `CurrencyFormatter.symbolForCode` موسّعين لنفس الـ١٦ عملة | ✅ |
| ٢ح | `UnifiedBankListener.moneyKeywords` — رموز/أكواد العملات الجديدة + مفردات فرنساوية (بنوك المغرب/الجزائر/تونس غالباً بتبعت بالفرنساوي) | ✅ |
| ٢ط | اختبارات: `+3 SaBankParserTest` لحل الغموض (رمز مميز/تطابق السوق/رفض عدم التطابق) | ✅ — 145/145 |

**القاعدة الذهبية اللي اتطبّقت فعلياً** (تأكيد المستخدم 2026-08-06): الـ fallback الدائم
لأي كشف غامض أو غير مؤكد هو السوق المحدد يدوياً في الـ onboarding — `TravelDetector`
(مرحلة ١) بترجع اقتراح بس أبداً مش تبديل صامت، و`extractCurrency` (فوق) بترفض تحسم
كلمة غامضة إلا لو بتطابق نفس السوق ده بالظبط. مفيش تداخل بين عملات الدول — كل رمز
مكتوب بشكل مميز (كود ISO أو اختصار محلي) بيتحل فوراً بدون أي اعتماد على السوق الحالي.

**فجوة تقنية موثّقة، مش قابلة للحل بصلاحيات `NotificationListenerService`**: طُلب "سحب
بيانات آخر ٢ شهر من الرسايل للتعلم من المصرف" عبر الإشعارات. `NotificationListenerService`
(المصدر اللي زاد بيستخدمه فعلاً بعد إسقاط `RECEIVE_SMS` في Phase A/A6) بيوفّر بس
`getActiveNotifications()` — الإشعارات النشطة/الغير متجاهَلة **دلوقتي**، مفيش API عام
يرجّع محتوى إشعارات قديمة اتشالت أو اتفتحت من أسابيع/شهور فاتت؛ نظام أندرويد نفسه
بيمسح المحتوى بعد التفاعل معاه، وميزة "Notification History" في الإعدادات مش متاحة
لتطبيقات خارجية أصلاً (تحتاج دور نظامي `Notification Assistant` مش `NotificationListenerService`
العادي). البديل المتوافق الموجود فعلاً في الريبو: `StatementImportScreen` (رفع كشف
حساب CSV من البنك يدوياً) — ده المسار المتاح لتاريخ معاملات قديم من غير أي صلاحية
حساسة إضافية. لو المطلوب فعلاً "تعلّم من آخر شهرين"، الخيار الوحيد المتوافق مع قرار
A6 هو توسيع `StatementImportScreen` (صيغ ملفات أكتر، أو ربط API بنكي رسمي Open Banking
لو متاح في السوق المستهدف) — مش قراءة إشعارات تاريخية، ده مش موجود تقنياً.

## مرحلة ٣ — المخزون الحي · ✅ خلصت (نطاق معدّل بعد تدقيق فعلي للكود)

قبل التنفيذ اتعمل تدقيق فعلي (Explore agent) — اتضح إن `InventoryFlowEngine` و
`ConsumptionLearner` **موجودين وشغالين فعلاً** (حقن فاتورة مصوّرة، تنبؤ استهلاك،
كشف ركود Task 23)، مش أسماء نظرية من الخطة. الفجوة الحقيقية كانت في حاجتين
تحديداً — بالظبط الاتنين اللي الخطة سمّتهم:

| # | البند | الحالة |
|---|------|--------|
| ٣أ | **معاملة بقالة كانت بتتصنّف وتقف** — `SaBankParser` بيصنّفها "البقالة" لأغراض الميزانية بس، مفيش وصل بالمخزون خالص. `ZadViewModel` بقى بيراقب `dao.getAllTransactions()` (نفس مصدر الحقيقة، بنكي أو يدوي مفيش فرق) ولما معاملة بقالة جديدة تظهر، بيفتح `_pendingGroceryPurchase` — `GroceryPurchasePromptDialog` جديد على `HomeScreen` بيسأل "اشتريت من X بـ Y — ضيف إيه؟" بشيبس من المخزون الحالي (تاب = +1) + حقل نص لصنف جديد. كل إضافة بتعيد استخدام `InventoryFlowEngine.injectScannedItems` بالظبط — نفس مسار حقن الفاتورة المصوّرة، مفيش منطق مواز | ✅ |
| ٣ب | **"المتابعة الدورية" كانت إشعار نصي بس** — `getCheckInCandidates()` كانت موجودة، بس بتولّد `AppNotification` بعنوان "🤔 هل خلص X؟" يطلب من المستخدم يفتح شاشة المخزون ويحدّث يدوياً. `InventoryCheckInCard` جديد على `HomeScreen` — سؤال تفاعلي فوري بـ٣ أزرار: **−1** (`consumeItem` بوحدة واحدة)، **خلص** (`consumeItem` بالكمية كاملة)، **لسه** (تأجيل ٣ أيام عبر `ConsumptionLearner.snoozeCheckIn` الجديدة — من غيرها السؤال كان هيرجع فوراً في أي إعادة حساب تالية) | ✅ |
| ٣ج | اختبارات: `+3 InventoryFlowEngineTest` (مرشّح متوقع النفاد، snooze بيشيله، صنف من غير snooze) | ✅ — 148/148 |

**قرار تصميم**: مفيش تعديل لسكيمة `AppNotification` (title/message بس، مفيش دعم
أزرار) — بدل توسيعها، الكاردين الجداد مبنيين مباشرة على `StateFlow`s في `ZadViewModel`
(نفس نمط `pendingSubscriptions`/`DetectedSubscription` الموجود بالفعل لتأكيدات
الاشتراكات المكتشفة) — مفيش كتابة صامتة في الحالتين، كل تغيير مخزون بيحصل بفعل مستخدم
صريح (تاب على زرار).

## مرحلة ٤ — اللوكيشن والـ geofence · ✅ خلصت (نطاق معدّل بعد تدقيق فعلي للكود)

قبل التنفيذ اتعمل تدقيق فعلي (Explore agent) للكود الموجود بدل الافتراض — اتضح إن
الـ geofencing **حقيقي وشغال فعلاً** (`GroceryGeofenceManager`/`GeofenceBroadcastReceiver`/
`GeofenceRefreshWorker`)، مش خطة نظرية زي ما بند "تشغيل الـ geofence" كان ممكن يوهم.
الفجوة الحقيقية كانت في ٤ حاجات تحديداً، مش في وجود الميزة من عدمه:

| # | البند | الحالة |
|---|------|--------|
| ٤أ | **العقل كان أعمى عن المكان تماماً** — دخول نطاق محل كان بيولّد إشعار محلي بس، zad-brain عمره ما كان بيعرف. `GeofenceBroadcastReceiver.notifyBrain()` جديدة بتنادي `zad-brain` (`trigger=geofence_enter`) بعد الإشعار المحلي مباشرة — نفس نمط `UnifiedBankListener` (نداء Supabase من مسار خلفية من غير ViewModel)؛ فشلها (لا إنترنت وقت الدخول) ما يأثرش على الإشعار المحلي | ✅ |
| ٤ب | **الدعوة كانت مدفونة فعلاً** — التفعيل كان موجود بس جوه `NearbyDealsScreen` بس، محدش يعرف إنه موجود من غير ما يدخل الشاشة دي بالذات. `LocationAlertsCard` جديد أعلى `HomeScreen` (نفس نمط `NotificationPermissionCard` الموجود) — تجاهل بيتفتكر دائماً (ميزة اختيارية، مش زي إذن إشعارات البنك). التحكم في الإيقاف لسه في `NearbyDealsScreen` بس، مش اتكرر | ✅ |
| ٤ج | **الموقع كان كاش قديم، مش قراءة حية** — `LocationManager.getLastKnownLocation()` في المكانين (`GroceryGeofenceManager.refreshGeofences`، `NearbyDealsScreen.searchNearby`) بيرجع فاضي على جهاز لسه ما فتحش خرائط، أو رقم قديم بالساعات. `LocationHelper` جديد (`FusedLocationProviderClient.getCurrentLocation()`) بيطلب إحداثية فعلية، مستخدم في المكانين | ✅ |
| ٤د | `LOCATIONIQ_API_KEY` — **مش قابل للتأكيد عبر MCP** (مفيش أداة تقرأ قيم الأسرار، بقصد أمني). الكود آمن في الحالتين: `GroceryGeofenceManager.findStores()` بيرجع لـ `OverpassRepo` تلقائياً لو LocationIQ رجع فاضي — مفيش أثر على الوظيفة سواء المفتاح متظبط أو لأ، بس الدقة تفرق. لو عايز تأكيد قاطع، لازم يتفحص من Supabase dashboard مباشرة | ⚠️ غير قابل للفحص من هنا |

**تعمّد عدم بناؤه** (قرار موثّق 2026-08-06، مش نقص): رصد تنقل/رحلة (زي ركوب أوبر أو
التحرك من مكان لمكان) عبر `ActivityRecognition` API. اتقيّم واتُرفض — تتبع خلفية
مستمر يعني استهلاك بطارية عالي، مراجعة Play Store أشد بكتير لتطبيق مالي، وحساسية
خصوصية حقيقية (تتبع تنقل شخص) مقابل عائد صغير. الـ geofence الموجود (دخول/خروج نطاق
محل بعينه) كافي لسيناريو "أنت في كارفور دلوقتي" من غير الحاجة لتتبع مستمر بين الأماكن.

ملاحظة Play Store لسه قايمة: `ACCESS_BACKGROUND_LOCATION` يحتاج نموذج إفصاح + فيديو
شرح لمراجعة جوجل (قرار Play Console إداري، خارج نطاق كود) — شوف CLAUDE.md.

## مرحلة ٥ — الواجهة والـ agent الحي · 🟡 جزئية (٥أ خلصت، ٥ب لسه)

| # | البند | الحالة |
|---|------|--------|
| ٥أ | **Live Agent Widget** — `ZadVoiceFab` الداخلي استُبدل بـ blob عضوي حي (Canvas، مش أيقونة/دايرة ثابتة). نفس تدرّج `ZadHeroGradient` بتاع كارت الميزانية بالظبط (مش لوحة ألوان جديدة منفصلة عن الهوية). ٤ حالات فعلية مربوطة بمنطق حقيقي موجود أصلاً (`isRecording`/`isProcessing`) + `isSpeaking` جديدة (عبر `UtteranceProgressListener` على الـ TTS، مكانتش موجودة قبل كده): **IDLE** (تنفس هادي + رمشة دورية)، **LISTENING** (تنفس أسرع + حلقة صوت متوسّعة + عيون واسعة)، **THINKING** (دوران داخلي أسرع + نظرة يمين-شمال)، **SPEAKING** (نبضة عين سريعة). الـ API العام (`ZadVoiceFab(modifier, viewModel)`) فضل زي ما هو — نقطة الاستدعاء الوحيدة في `HomeScreen` متلمستش | ✅ |
| ٥أ-تحقق | Roborazzi screenshots فعلية للحالات الأربعة (`app/build/outputs/roborazzi/zad_live_agent_*.png`) — اتشافت بصرياً قبل ما تتقفل، مش افتراض إن الكود صح لمجرد إنه بيتصرّف | ✅ — 152/152 tests |
| ٥ب | **إعادة تصميم بصرية شاملة** (glassmorphism/blur حقيقي، Corner Radii 24-32dp لكل الكروت، أيقونات براند حقيقية للاشتراكات زي نتفليكس، أنيميشن مخصص لكل عنصر، Figma MCP من الصفر) | ⬜ **مؤجلة عمداً** |

**سبب تأجيل ٥ب في حينه**: طلب مفتوح النطاق بيغطي كل شاشة وكل كارت في التطبيق — عكس
كل مرحلة فاتت (نطاق محدد، قابل للتحقق ببناء واحد). محاولة تنفيذه في commit واحد
بتخالف قاعدة CLAUDE.md "One task, one commit" وبتمنع أي تحقق بصري حقيقي قبل الالتزام
بيه. الحل: تقسيم كارت-كارت، كل واحد بتحققه البصري بتاعه — مش دفعة واحدة.

### ٥ب-١ — كارت الميزانية الرئيسي (`ZadCardHero`) · ✅ خلصت

| # | البند | الحالة |
|---|------|--------|
| ١ | بقعة ضوء زجاجية مموّهة (`zadGlassBlur`، نفس الأداة الموجودة أصلاً في `GlassCard`) أعلى الكارت — إحساس عمق حقيقي فوق الجراديانت المسطّح، مش لون واحد فلات. API 31+ بس (fallback no-op تحت كده، نفس نمط `GlassCard` الموجود) | ✅ |
| ٢ | الـ 28dp corner radius الأصلي أكبر من الـ 24dp المطلوب — اتسيب زي ما هو، مفيش داعي يتصغّر | ✅ |
| ٣ | **شريط تقدّم جديد** (مصروف/سقف الشهر) — عنصر مكانش موجود خالص قبل كده. ٣ ألوان حسب النسبة: أخضر فاتح (<70%)، كهرماني (70-90%)، أحمر (>90%) — نفس عتبات `ZadCentralBrain`/`BudgetTracker` الموجودة أصلاً لتنبيهات تخطي الميزانية، مش أرقام مخترعة. بيختفي تماماً لو `monthlyLimit <= 0` (نسبة من سقف مش معروف بلا معنى) | ✅ |
| ٤ | `BudgetSetupPromptCard` (الحالة الفاضية، Task 19.0 معيار ٦) اتاخدت نفس معاملة الزجاج — عشان تحس إنها نفس عائلة الكارت الرئيسي البصرية، مش شاشة تانية مختلفة الطابع | ✅ |
| ٥ | تحقق بصري فعلي (مش افتراض): ٥ Roborazzi captures — الكارت بحالته العادية، شريط أحمر (95%)، شريط كهرماني (75%)، الشريط مختفي (بدون سقف)، والحالة الفاضية — كلهم اتشافوا قبل الالتزام | ✅ — 156/156 tests |

**ملاحظة RTL**: شريط التقدّم بيتملى من اليمين لليسار تلقائياً (Compose's `fillMaxWidth(fraction)`
بيحترم `LayoutDirection` بدون أي كود إضافي) — نفس اتجاه بقعة الضوء (`Alignment.TopStart`
بترسم يمين في RTL). اتأكد بصرياً من الـ screenshots، مش افتراض.

### ٥ب-٢ — كروت اللوكيشن والتنبيهات الميدانية · ✅ خلصت

| # | البند | الحالة |
|---|------|--------|
| ١ | `LocationAlertsCard` (كارت HomeScreen) — نفس عائلة الزجاج: زوايا 24dp (بدل 16dp الافتراضية)، خلفية جراديانت هادية (`primary`→`tertiary` بشفافية منخفضة، مش نفس أخضر الهيرو حرفياً عشان ميتنافسش بصرياً معاه)، بقعة `zadGlassBlur` | ✅ |
| ٢ | دبوس مكان عائم — شارة دائرية بظل حقيقي (`Modifier.shadow`) بدل أيقونة مسطّحة على الخلفية، في `LocationAlertsCard` وفي `NearbyStoreCard` (كروت المتاجر جوه `NearbyDealsScreen`) الاتنين | ✅ |
| ٣ | زرار جديد "شوف اللي قريب مني" (`onOpenNearby`) على `LocationAlertsCard` — قبل كده الكارت مكانش بيوصل لشاشة "زاد القريب" خالص، بس تفعيل/تجاهل. `HomeScreen` أخد `onNavigateToNearby` جديدة، متوصّلة في `MainScreen` بـ`go(ZadRoutes.DEALS)` | ✅ |
| ٤ | `NearbyStoreCard` بقت 24dp (كانت 16dp) + أيقونة اتجاه صغيرة (`NearMe`) جنب المسافة — تسلسل هرمي أوضح (اسم المحل كبير، مسافة+أيقونة تحتها، تنبيه النواقص ملوّن) | ✅ |
| ٥ | تحقق بصري فعلي: ٣ Roborazzi captures (كارت اللوكيشن، كارت سوبرماركت، كارت صيدلية) — `NearbyStoreCard` بقت `internal` بدل `private` عشان الاختبار يوصلها مباشرة (نفس الشاشة `stateful`، مش قابلة للـ capture كاملة) | ✅ — 159/159 tests |

**ملاحظة**: خلفية `LocationAlertsCard` عمداً مش نفس تدرّج `ZadHeroGradient` الأخضر —
الكارت ده بيظهر تحت كارت الميزانية مباشرة على `HomeScreen`، فلو استخدم نفس التدرّج
هيقرأ كإطالة للهيرو مش كارت منفصل. "عائلة بصرية واحدة" هنا معناها نفس *الأداة*
(`zadGlassBlur`) ونفس *نصف القطر*، مش نفس اللون بالظبط.

### ٥ب-٣ وما بعدها — باقي الكروت (المخزون، الاشتراكات بأيقونات براند...) · ⬜

---

## قواعد شغل على الخطة دي

- مرحلة = commit واحد + تقرير.
- مفيش "تمّت" من غير مخرجات بناء بعد التعديل (قاعدة CLAUDE.md).
- الجدول فوق هو مصدر الحقيقة للحالة — يتحدّث في نفس الـ commit بتاع المرحلة.
