# GEMINI_API_KEY: OPTIONAL. All AI features (chat, insights, receipt/inventory photo
# scanning) run on GROQ_API_KEY server-side in the zad-ai-proxy Supabase Edge Function —
# that key lives in Supabase project secrets, NOT here. This key only powers an unused
# fallback client path (ZadAiGeminiClient) if a user manually pastes a Gemini key in the
# app's camera screen. Leave the placeholder below if you don't have one; nothing breaks.
GEMINI_API_KEY=MY_GEMINI_API_KEY
# SUPABASE_URL: Your Supabase project URL (required)
SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_ANON_KEY: Your Supabase anonymous public key (required)
SUPABASE_ANON_KEY=your-anon-key
